# App Demo Firebase Auth

App demo de login e cadastro usando Firebase (Email/Password) com React Native + Expo.  
Suporta **TypeScript**.

## Como rodar

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start
   ```
